#include "test.h"

int main(int argc, char *argv[])
{
    //video_test(argc, argv);
    //socket_test4(argc, argv);
    //pipe_test(argc, argv);
    //shm_test(argc, argv);
	//trig_test(argc, argv);

	xlibc_test(argc,argv);
    
    return 0;
}
