#include "test.h"

int main(int argc, char *argv[])
{
    //socket_test4(argc, argv);
    //pipe_test(argc, argv);
    //shm_test(argc, argv);
	//trig_test(argc, argv);

    //trig_test(argc, argv);
    //math_test(argc, argv);
    //http_test(argc, argv);
    //xlibc_test(argc, argv);
    //buddy_test(argc, argv);
    cjson_main();
    return 0;
}
