mount -o loop /tmp/d.img /newfs
ls newfs
unmount /newfs
mount -o loop /tmp/d.img /newfs
ls newfs
unmount /newfs
mount -o loop /tmp/d.img /newfs
ls newfs
unmount /newfs
mount -o loop /tmp/d.img /newfs
ls newfs
unmount /newfs
mount -o loop /tmp/d.img /newfs
ls newfs
unmount /newfs

#mkfs -t fat16 /dev/ram0
#mount /dev/ram0 /newfs
#ls newfs
#unmount /dev/ram0
#mount /dev/ram0 /newfs
#ls newfs
#unmount /newfs
#mkfs -t fat16 /dev/ram0
#mount /dev/ram0 /newfs
#ls newfs
#unmount /dev/ram0
#mount /dev/ram0 /newfs
#ls newfs
#unmount /newfs